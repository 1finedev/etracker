const Loading = () => {
  // this full page shows on page load while we are checking if user is authenticated, kindly modify Markup
  return (
    <div className="w-full h-[100vh] flex items-center justify-center">
      Loading... please wait
    </div>
  );
};

export default Loading;
